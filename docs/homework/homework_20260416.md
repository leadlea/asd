# 作業ログ: 2026-04-16（MTGフィードバック反映 + Feature Dose-Response実験）

## 背景
2026-04-15 MTGで合意された論文最終仕上げタスク（paper-finalization spec）の完了後、
MTG中の議論を踏まえた追加修正とFeature Dose-Response実験の設計・実装を実施。

## 完了した作業

### 1. LLM循環的相互行為検証スクリプトの修正
- `scripts/analysis/llm_circular_interaction.py`:
  - Bedrock inference profile ID の自動解決（`_resolve_inference_profile()`追加）
  - Anthropicモデルの`seed`パラメータ非対応への対処
- dry-run + 実API実行の両方で動作確認済み
- 実行結果: 天井・床効果（C=高→4.000, C=低→0.000）で方向性整合は未確認
  → LLM生成対話ではなく実データ操作のDose-Response実験に方針転換

### 2. CONSORTフローチャートの修正
- **[要確認]プレースホルダーを全て実数値に置換**:
  - CEJC全体: 577会話
  - home2サブセット（場所=自宅, 話者数=2）: 140会話（437除外）
  - 会話×話者ペア展開: 378ペア
  - HQ1フィルタ後: 120ペア（258除外）
- **HQ1フィルタの閾値を明記**（paper1_ja.tex 2.1.2節）:
  1. 発話ペア数 ≥ 80
  2. テキスト文字数 ≥ 2,000
  3. 質問直後ペア数 ≥ 10
- **横長レイアウトに変更**（論文掲載用）
- home2選定条件と3者会話の脚注を追加

### 3. Supplementaryへのプロンプト掲載
- paper1_ja.tex 付録セクションを全面改訂:
  - 条件1: System + User プロンプトテンプレート全文
  - 条件2: 統計情報テキスト生成テンプレート + 4統計量の算出方法
  - 条件3: Derangementアルゴリズム（5ステップ）
  - IPIP-NEO-120の項目構成（各次元の代表例、正方向/逆転の説明）

### 4. Feature Dose-Response実験パイプラインの実装
- **spec作成**: `.kiro/specs/feature-dose-response/` (requirements.md, design.md, tasks.md)
- **3段階設計**: ×0（全削除）, ×1（元データ）, ×3（3倍）
  - 根拠: CEJCフィラー数中央値13.5個。×0.5/×2.0では操作量差が不十分
- **新規スクリプト5本**:
  - `scripts/dose_response/manipulate_text.py` — テキスト操作の純粋関数群
  - `scripts/dose_response/gen_dose_monologues.py` — Dose Level別parquet生成
  - `scripts/dose_response/verify_features.py` — 特徴量検証
  - `scripts/dose_response/prepare_ensemble_dirs.py` — ensemble互換ディレクトリ構築
  - `scripts/dose_response/dose_response_report.py` — 用量反応分析レポート
- **シェルスクリプト2本**:
  - `scripts/dose_response/run_scoring_dose.sh` — LLM採点コマンド
  - `scripts/dose_response/run_ensemble_dose.sh` — ensemble実行コマンド
- **Makefileターゲット追加**: `dose-manipulate`, `dose-verify`, `dose-dirs`, `dose-report`, `dose-response`
- **動作確認**: dry-runで全パイプライン正常動作
- **特徴量検証結果（FILL）**: ×0=0.0, ×1=7.7, ×3=23.1（完全な単調非減少）

### 5. 論文PDF/DOCX更新
- Discussion 4.5節: `★結果差し替え予定★` マーカー付きプレースホルダー
- `paper1_ja.pdf` (47ページ) + `reports/paper1_ja.docx` を再生成

## 進行中（LLM採点実行中）
- FILL ×0 / ×3 の2条件を並列実行中（4教師 × 5trait × 120話者 × 24項目）
- 推定所要時間: 約40-60時間
- レジューム機能あり（接続切断時は同一コマンド再実行で途中から再開）

## 次のステップ
1. FILL採点完了後 → `make dose-dirs` → ensemble実行 → `make dose-report`
2. 結果に基づきDiscussion 4.5節のプレースホルダーを差し替え
3. YESNO / OIR（統制条件）の採点・分析
4. 山下先生のdocx編集内容をLaTeXに反映

### 6. 表5（コーパス基本情報との関連分析）の列削減
- 宗田さんの指摘: Pearson rとSpearman ρが同じパターンでビジー
- **Spearman ρのみ残し、Pearson r列を削除**
  - 理由: 特徴量の多くが非正規分布（OIR率の右裾、フィラー率の右裾）
  - 有意/非有意の判定は両方で完全一致
- 表が7列→5列（Feature, U, p_gender, ρ_Spearman, p_Spearman）に簡素化
- Method 2.4節: 「Spearman ρを主報告、Pearsonでも同様の結果」と記述更新
- Results 3.3節: 本文中の年齢相関値をSpearman ρに統一
- PDF/DOCX再生成済み
